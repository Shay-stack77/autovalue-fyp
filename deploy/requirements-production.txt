# Production-only deps (exclude dev/test/notebook tooling)
# Install on the SoC web server with: pip install -r deploy/requirements-production.txt

# Core data + ML
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2

# Web framework
flask==3.0.3
flask-cors==5.0.0

# Production WSGI servers — install whichever the host supports
gunicorn==23.0.0   ; sys_platform != "win32"
waitress==3.0.0    ; sys_platform == "win32"

# Live scraper (optional; only needed if /refresh is wired up)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
